<%= render 'article' %>
  <div style="padding:15px;">
    <%= link_to 'Delete', article_path(@article), 
      :confirm => "Are you sure you want to delete this article?", 
      :method => :delete, :action => "destroy", :class=>'muted' %>&nbsp;
  <%= link_to 'Edit', edit_article_path(@article), :class=>'muted' %> &nbsp;
  <%= link_to 'Back', root_path, :class=>'muted' %>
</div>
